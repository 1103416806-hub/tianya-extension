# 填呀 0.1

面向反复填写校招网申的学生；以浏览器侧边栏作为操作面。沿用现有填呀钴蓝品牌，选择安静、清晰的编辑工具风格，而非营销页面。视觉记忆点是「简历事实 → 网页字段」可核对的逐行映射。

字体：Bahnschrift 标题数字 / Microsoft YaHei 与苹方中文。蓝 #2455eb，文字 #17233b，边框 #e1e6ef，画布 #f5f7fb。44px 控件、8px 圆角、16px 边距。横向桌面预览不放大为大卡片；保持侧栏阅读宽度。图标用小 SVG 和文字，不需要生成位图。React + esbuild 打包为本地 MV3 脚本，避免远程代码和开发服务器依赖。

交互：公司目录 → 简历校对 → 当前页识别 → 确认填写 → 用户提交。空白字段才允许写入，缺失信息保留空白，敏感和同意类字段不自动处理，撤销仅恢复仍等于插件写入值的字段。AI 明确可选，云端发送前单独确认。

0.1.2 窄范围流程修正：保留现有蓝色、字号与侧栏，上传后立即给出本地初步结果，把姓名/手机/邮箱和姓名来源放在上传区下方。首次导入写入可编辑草稿；已有资料时只预览，显式确认才替换。明确区分文件文字提取、本地规则初步识别、人工校对三件事，不把规则包装为 AI，不展示未经测量的准确率。沿用现有组件，不需要位图概念图。
